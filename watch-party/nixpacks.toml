[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages.pnpm"]

[phases.install]
cmds = ["npm ci --omit=dev || npm install"]

[phases.build]
cmds = ["npm run build:railway"]

[start]
cmd = "npm run preview"
